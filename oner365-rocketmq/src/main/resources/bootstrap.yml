
server: 
  port: 8713

spring: 
  application:
    name: oner365-rocketmq
    
  profiles:
    active: dev
    
  cloud:
    nacos:
      config:
        namespace: ${spring.profiles.active}
        group: DEFAULT_GROUP
        file-extension: yml
        shared-configs:
          - application-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}
     

