
server: 
  port: 8725

spring: 
  application:
    name: oner365-zookeeper
    
  profiles:
    active: dev
    
  cloud:
  
    zookeeper:
      connect-string: localhost:2181
      connection-timeout: 10s
      session-timeout: 60s
      base-sleep-time-ms: 5000
      max-retries: 10
     
logging:
  file:
    path: /Users/zhaoyong/Downloads/logs